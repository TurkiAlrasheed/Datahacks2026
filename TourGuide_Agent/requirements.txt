anthropic>=0.42.0
python-dotenv>=1.0.0

# Classifier inference — match the versions used by species_identification/.
torch>=2.0
torchvision>=0.15
Pillow>=9.0

# Voice I/O (ElevenLabs STT + TTS, mic capture, speaker playback).
elevenlabs>=2.0
sounddevice>=0.4
soundfile>=0.12

# Location detection + reverse geocoding (ipapi.co + OSM Nominatim).
requests>=2.28

# Optional — only needed if a GPS module is wired to the Arduino UNO Q over
# serial (USB/UART). `location.py` imports these lazily and falls back to
# IP geolocation if they're missing.
# pyserial>=3.5
# pynmea2>=1.19
